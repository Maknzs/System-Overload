# Backend environment
MONGODB_URI=mongodb+srv://<user>:<pass>@systemoverload.kolaug1.mongodb.net/?retryWrites=true&w=majority&appName=SystemOverload
JWT_SECRET=change_me
PORT=8080

# Session cookie (for incremental migration to Better Auth)
SESSION_COOKIE_NAME=so_session

# Better Auth
# Used if you enable the Better Auth integration. Generate a strong value.
BETTER_AUTH_SECRET=change_me_please
# Comma-separated list of allowed frontend origins for Better Auth
# Example (Vite dev server):
# BETTER_AUTH_TRUSTED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
BETTER_AUTH_TRUSTED_ORIGINS=

# Optional: email transport for feedback
# If SMTP_* are not set, emails are logged to console (jsonTransport)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=System Overload <no-reply@example.com>
MAIL_TO=owner@example.com
# Optional rollout phase for feature flags: development | staging | production
# If unset, falls back to NODE_ENV mapping (production -> production; else development)
APP_PHASE=development
