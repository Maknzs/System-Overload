# Frontend environment sample

# API base (optional). In dev, Vite proxy already maps /api to localhost:8080.
# VITE_API_BASE=http://localhost:8080/api

# App rollout phase separate from Vite MODE; used for toggling dev UI/features
# Options: development | staging | production
VITE_APP_PHASE=development

